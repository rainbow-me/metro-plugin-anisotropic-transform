{
  "name": "EvilKeychain",
  "version": "1.0.0",
  "description": "A project which tries to use react-native-keychain, and it really shouldn't.",
  "main": "dist",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "yarn && babel src --out-dir dist && rm -rf node_modules"
  },
  "dependencies": {
    "react-native-keychain": "^6.2.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.12.10",
    "@babel/core": "^7.12.10",
    "@babel/preset-env": "^7.12.11",
    "@babel/preset-react": "^7.12.10",
    "husky": "^4.3.6",
    "jest": "^26.6.3",
    "leasot": "^11.4.0",
    "prettier": "^2.2.1",
    "react-native": "^0.63.4"
  }
}
