{
  "name": "TrustedKeychain",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "android": "react-native run-android",
    "ios": "react-native run-ios",
    "start": "rm -rf node_modules; yarn; echo \"Starting bundler.\" ;react-native start --reset-cache > debug.log",
    "test": "jest",
    "lint": "eslint ."
  },
  "dependencies": {
    "react": "16.13.1",
    "react-native": "0.63.4",
    "react-native-dangerous-keychain": "../react-native-dangerous-keychain/",
    "react-native-keychain": "^6.2.0"
  },
  "devDependencies": {
    "@babel/core": "^7.12.10",
    "@babel/runtime": "^7.12.5",
    "@react-native-community/eslint-config": "^2.0.0",
    "babel-jest": "^26.6.3",
    "deepmerge": "^4.2.2",
    "eslint": "^7.15.0",
    "jest": "^26.6.3",
    "metro-plugin-anisotropic-transform": "../../",
    "metro-react-native-babel-preset": "^0.64.0",
    "react-test-renderer": "16.13.1"
  },
  "jest": {
    "preset": "react-native"
  }
}
